name: device-intelligence-bundle

services:
  php:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: device-intelligence-bundle-php
    working_dir: /app
    volumes:
      - .:/app
      - composer-cache:/root/.composer/cache
      - coverage-data:/app/coverage
    environment:
      - COMPOSER_ALLOW_SUPERUSER=1
      - COMPOSER_ROOT_VERSION=1.0.1-dev
      - XDEBUG_MODE=coverage
    command: ["tail", "-f", "/dev/null"]
    networks:
      - device-intelligence-bundle-network

volumes:
  composer-cache:
    driver: local
  coverage-data:
    driver: local

networks:
  device-intelligence-bundle-network:
    driver: bridge
