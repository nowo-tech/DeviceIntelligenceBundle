# Web Profiler

Panel id: `nowo_device_intelligence`. Twig namespace: `NowoDeviceIntelligenceBundle`.

Shows device id, new/existing, confidence, similarity, stability, risk score/level, truncated signal summaries, risk reasons, and phase timings.

Raw high-entropy values and observation token secrets are not dumped.
