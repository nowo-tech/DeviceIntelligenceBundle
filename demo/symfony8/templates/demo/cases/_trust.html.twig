<p class="small text-muted">{{ 'demo.case.trust.logic'|trans }}</p>
{% if not app.user %}
    <a class="btn btn-outline-primary" href="{{ path('demo_login', { _locale: app.request.locale }) }}">{{ 'demo.login.nav'|trans }}</a>
{% else %}
    {{ include('demo/_form.html.twig', { form: trust_form }) }}
    {{ include('demo/_form.html.twig', { form: revoke_form }) }}
{% endif %}
