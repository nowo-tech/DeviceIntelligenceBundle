{% extends 'base.html.twig' %}

{% block body %}
<div class="container py-5">
    <h1>{{ 'demo.heading'|trans }}</h1>
    <p class="lead">{{ 'demo.lead'|trans }}</p>
    <p>{{ 'demo.body'|trans }}</p>
    <pre id="collect-result" class="bg-white border p-3">{{ 'demo.waiting'|trans }}</pre>
</div>
{% endblock %}
