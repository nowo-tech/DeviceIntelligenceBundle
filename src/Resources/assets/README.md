# Browser assets

TypeScript collectors for Device Intelligence. Built IIFE lives in `src/Resources/public/js/`.

This is **not** a standalone npm package. From the repository root:

```bash
pnpm install
pnpm test
pnpm build
```
